// Shared configuration for the public site — single source of truth for the
// CRM Worker's base URL so it isn't duplicated (and left to drift) across
// script.js, contact-modal.js, and consent-form.html.
const AVITAL_API_BASE = 'https://avital-heal-crm.tgthf7frmp.workers.dev';
