// Shared configuration for the workshop brochure site — single source of
// truth for the CRM Worker's base URL so it isn't duplicated (and left to
// drift) across index.html and consent.html.
const AVITAL_API_BASE = 'https://avital-heal-crm.tgthf7frmp.workers.dev';
